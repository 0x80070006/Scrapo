version: '3.8'
services:
  film-scraper:
    build: .
    container_name: film-scraper-ultimate
    ports:
      - "5000:5000"
    volumes:
      - ./:/app
    environment:
      - PYTHONUNBUFFERED=1
    shm_size: 2gb
    restart: unless-stopped
